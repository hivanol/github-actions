name: test syn
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Name of the person to greet'
        required: true
        default: 'World'
    secrets:
      pass:
        required: true
        description: 'A secret value to pass to the workflow'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: SHow name
        run: echo inputs.name