name: test syn
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Name of the person to greet'
        required: true
        default: 'World'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        run: echo input.name